'use client';import {useActionState} from 'react';import {recordVettingAction} from './actions';export default function VettingForm({candidates}){const[state,action,pending]=useActionState(recordVettingAction,{});return <form action={action}><label>Candidate<select name="candidate" required><option value="">Select</option>{candidates.map(c=><option value={c.id} key={c.id}>{c.first_name} {c.last_name}</option>)}</select></label><label>Check<select name="kind">{['identity','education','employment','reference','work-sample','technical'].map(x=><option key={x}>{x}</option>)}</select></label><label>Status<select name="status">{['requested','candidate-provided','reviewed','verified','rejected'].map(x=><option key={x}>{x}</option>)}</select></label><label>Evidence / reviewer note<textarea name="evidence" required minLength={10} maxLength={3000}/></label><button disabled={pending}>{pending?'Saving…':'Record check'}</button><p role={state.error?'alert':'status'}>{state.error||state.message}</p></form>}

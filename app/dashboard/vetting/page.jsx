import {requireWorkspace} from '../../../lib/workspace-page';import {listRecords,pbFilterValue} from '../../../lib/pocketbase';import VettingForm from './VettingForm';export const dynamic='force-dynamic';export default async function Page(){const {workspace,membership}=await requireWorkspace();const filter=`workspace = "${pbFilterValue(workspace.id)}"`;const[candidates,checks]=await Promise.all([listRecords('candidates',{filter:`${filter} && status = "active"`,perPage:200}),listRecords('candidate_vetting',{filter,sort:'-created',perPage:100})]);return <div className="productPage"><div className="pageHeading"><div><p className="pageEyebrow">EVIDENCE / VETTING</p><h1>Human verification desk</h1><p>Record what was checked, by whom, and on what evidence. “Verified” is never inferred by AI.</p></div></div><div className="settingsGrid"><section className="surfaceCard settingsCard"><h2>New check</h2>{['owner','admin','recruiter'].includes(membership.role)?<VettingForm candidates={candidates.items}/>:<p>View-only access.</p>}</section><section className="surfaceCard settingsCard"><h2>Recent checks</h2>{checks.items.length?checks.items.map(x=>{const c=candidates.items.find(y=>y.id===x.candidate);return <article key={x.id}><b>{c?`${c.first_name} ${c.last_name}`:'Candidate'} · {x.kind}</b><p>{x.status} — {x.evidence}</p></article>}):<p>No checks recorded.</p>}</section></div></div>}

import Link from 'next/link'; import {notFound} from 'next/navigation'; import {getCandidateForWorkspace} from '../../../../../lib/recruit-data'; import {requireWorkspace} from '../../../../../lib/workspace-page'; import EmailForm from './EmailForm';
const templates={custom:['',''], 'interview-invite':['Interview invitation','Hi {{first_name}},\n\nWe would like to invite you to the next conversation for {{role}}. Please reply with your availability.\n\nRegards,\nRecruit AI'], 'application-update':['Application update','Hi {{first_name}},\n\nThank you for your interest in {{role}}. We wanted to share an update on your application.\n\nRegards,\nRecruit AI'], rejection:['Update on your application','Hi {{first_name}},\n\nThank you for the time you invested in our process. We have decided not to move forward at this time.\n\nRegards,\nRecruit AI'], 'follow-up':['Following up','Hi {{first_name}},\n\nWe are following up regarding your application for {{role}}.\n\nRegards,\nRecruit AI']};
export default async function CommunicationPage({params,searchParams}){const {candidateId}=await params;const query=await searchParams;const {workspace}=await requireWorkspace();const candidate=await getCandidateForWorkspace(workspace.id,candidateId);if(!candidate)notFound();const blocked=candidate.consent_status==='withdrawn'||candidate.status==='do-not-contact';return <div className="productPage"><Link className="backLink" href={`/dashboard/candidates/${candidate.id}`}>&larr; Back to candidate</Link><section className="surfaceCard communicationShell"><p className="pageEyebrow">CANDIDATE COMMUNICATION</p><h1>Send an email to {candidate.first_name}.</h1><p>Every send is recorded. Consent withdrawal and do-not-contact status block delivery.</p>{query?.notice&&<div className="analysisNotice">{query.notice==='sent'?'Email sent and logged.':query.notice==='provider-missing'?'Email provider is not configured; failed attempt was logged.':query.notice==='blocked'?'Sending is blocked for this candidate.':'Complete all fields or try again.'}</div>}<EmailForm candidate={candidate} templates={templates} blocked={blocked}/></section></div>}

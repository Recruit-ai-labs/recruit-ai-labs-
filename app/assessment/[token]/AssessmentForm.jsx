'use client';import {useActionState} from 'react';import {submitAssessmentAction} from './actions';export default function Form({assessment}){const[s,a,p]=useActionState(submitAssessmentAction,{});if(s.message)return <section><h1>Thank you.</h1><p>{s.message}</p></section>;return <form action={a}><input type="hidden" name="token" value={assessment.token}/>{assessment.questions.map((q,i)=><label key={q.id}><b>{i+1}. {q.prompt}</b><textarea name={q.id} required minLength={30} maxLength={5000} rows={7}/></label>)}<button disabled={p}>{p?'Evaluating securely…':'Submit assessment'}</button>{s.error&&<p role="alert">{s.error}</p>}</form>}

const assert=require('node:assert/strict');
async function main(){const{chromium}=require(process.env.PLAYWRIGHT_MODULE||'playwright');const browser=await chromium.launch({headless:true});try{const page=await browser.newPage();await page.route('**/fonts.googleapis.com/**',r=>r.abort());await page.route('**/fonts.gstatic.com/**',r=>r.abort());const results=[];for(const width of [320,390,768,1280]){await page.setViewportSize({width,height:844});await page.goto(process.env.PUBLIC_BASE_URL||'http://localhost:3000',{waitUntil:'domcontentloaded'});await page.getByRole('button',{name:'Open menu'}).waitFor({state:width<=760?'visible':'attached'});results.push(await page.evaluate(()=>({viewport:innerWidth,scroll:document.documentElement.scrollWidth,culprits:[...document.querySelectorAll('body *')].filter(e=>e.getBoundingClientRect().right>innerWidth+2&&getComputedStyle(e).visibility!=='hidden').slice(0,6).map(e=>e.className||e.tagName)})));if(width===390){await page.getByRole('button',{name:'Open menu'}).click();await page.getByRole('navigation',{name:'Mobile navigation'}).getByRole('link',{name:'Pricing',exact:true}).click();assert.equal(await page.getByRole('button',{name:'Open menu'}).getAttribute('aria-expanded'),'false');await page.screenshot({path:'D:/Temp/recruit-mobile-landing.png',fullPage:true});}}console.log(JSON.stringify(results,null,2));assert(results.every(r=>r.scroll<=r.viewport+1),'Public page has horizontal overflow');}finally{await browser.close()}}
main().catch(e=>{console.error(e);process.exitCode=1});
